table:
  name: venue_category
  schema: public
is_enum: true
array_relationships:
  - name: venues
    using:
      foreign_key_constraint_on:
        column: category
        table:
          name: venues
          schema: public
